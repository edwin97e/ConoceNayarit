<%= form_with(model: sitecategory, local: true) do |form| %>
  <% if sitecategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sitecategory.errors.count, "error") %> prohibited this sitecategory from being saved:</h2>

      <ul>
      <% sitecategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: site, local: true) do |form| %>
  <% if site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  
  <div class=¨form-group¨>
    <%= form.label "Tipo de Sitio" %><br>
    <%= form.select :sitecategory_id ,options_from_collection_for_select(Sitecategory.all,:id, :name) ,prompt: "Seleccione un Tipo", class:"form-control"%>       
</div>  

  <div class="field">
    <%= form.label :body %>
    <%= form.cktext_area :body %>
  </div>
  
  <div class="field">
    <%= form.label :imagen %>
    <%= form.file_field :img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
